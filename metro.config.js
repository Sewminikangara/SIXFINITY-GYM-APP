const { getDefaultConfig } = require('@expo/metro-config');

/**
 * Metro configuration
 * https://docs.expo.dev/workflow/using-metro/
 *
 * @type {import('expo/metro-config').MetroConfig}
 */
module.exports = getDefaultConfig(__dirname);
